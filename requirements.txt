# DBJavaGenix - 核心运行时依赖
# 推荐使用: pip install -e ".[dev]"  或  uv sync --extra dev
# 此文件为兼容场景保留, 与 pyproject.toml 保持一致

# MCP 协议
mcp>=1.6.0

# 数据验证
pydantic>=2.10.0

# 数据库
sqlalchemy>=2.0.36
pymysql>=1.1.1
psycopg2-binary>=2.9.10

# 配置
PyYAML>=6.0.2
python-dotenv>=1.0.1

# 模板
pystache>=0.6.5

# 异步 / IO
aiofiles>=24.1.0
httpx>=0.27.0

# CLI
typer>=0.13.0
rich>=13.9.0

# AI 语义层 (Claude API)
anthropic>=0.40.0
