# DBJavaGenix 开发环境依赖
# `pyproject.toml` 是规范依赖入口；本文件保留给使用 pip 的开发环境。

-r requirements.txt

# 测试
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0

# Lint / Format / Type
ruff>=0.8.0
mypy>=1.13.0
pre-commit>=4.0.0
