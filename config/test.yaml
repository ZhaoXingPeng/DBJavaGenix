# DBJavaGenix Test Configuration

# Database connection settings (使用完成计划中的测试数据库)
database:
  type: mysql
  host: 115.190.155.106
  port: 3309
  database: test_db
  username: root
  password: .Dd5779496
  charset: utf8mb4

# AI service configuration (暂时禁用)
ai:
  provider: openai
  api_key: "test-key"
  model: "gpt-3.5-turbo"
  temperature: 0.1
  max_tokens: 2000

# Code generation settings
generation:
  output_dir: "./test_output"
  package_name: "com.example.test"
  code_style: spring_boot
  template_engine: mustache
  mapping_tool: mapstruct
  author: "TestUser"
  
  # Entity settings
  entity_suffix: ""
  use_lombok: true
  use_jpa: true
  
  # DAO settings
  dao_suffix: "Mapper"
  use_mybatis_plus: true
  
  # Service settings
  service_suffix: "Service"
  service_impl_suffix: "ServiceImpl"
  
  # Controller settings
  controller_suffix: "Controller"
  use_swagger: true
  
  # Additional options
  generate_dto: true
  generate_vo: true
  generate_tests: false
  
  # MapStruct configuration
  mapstruct_component_model: "spring"
  mapstruct_unmapped_target_policy: "IGNORE"
  generate_mappers: true

# Application settings
debug: true
log_level: INFO
log_file: null